# Web API
fastapi>=0.115,<1
uvicorn[standard]>=0.32,<1

# Settings from environment (.env)
pydantic-settings>=2.6,<3

# Excel ingestion (open formats)
pandas>=2.2,<3
openpyxl>=3.1,<4

# Multipart file uploads (FastAPI UploadFile)
python-multipart>=0.0.12,<1

# Database ORM + SQLite driver (async-ready pattern later)
sqlalchemy>=2.0,<3
alembic>=1.14,<2

# Async HTTP client for calling local LLM APIs (e.g. Ollama) — agent steps ahead
httpx>=0.27,<1
